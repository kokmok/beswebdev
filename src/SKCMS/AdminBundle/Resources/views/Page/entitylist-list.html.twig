{%extends "SKCMSAdminBundle:Element/Template:admin-list.html.twig"%}

{#{%block adminContent%}#}

{#<a href="{{path('sk_admin_edit',{entity:entityParams.name})}}" class="btn btn-large btn-primary">Créer {{entityParams.beautyName}}</a>#}

{%block header2 %}Entity Lists {{parent()}}{%endblock%}

{%block th %}
<th>Name</th>
<th>Entity</th>
<th>limit</th>
<th>Order By</th>
<th>Order direction</th>
{{parent()}}
{%endblock%}

{%block tr %}
    <td>{{entity.name}}</td>
    <td>{{entity.entity}}</td>
    <td>{{entity.limit}}</td>
    <td>{{entity.orderBy}}</td>
    <td>{{entity.order}}</td>

    <td class="center">
        <a class="btn btn-info" href="{{path('sk_admin_entitylistedit',{id:entity.id})}}">
                <i class="icon-edit icon-white"></i>  
                Edit                                            
        </a>
        <a class="btn btn-danger btn-setting" data-content="Voulez vous vraiment supprimer cette entité ?" data-title="Confirmation de suppression" data-confirm="Oui" href="{{path('sk_admin_entitylistdelete',{id:entity.id})}}">
                <i class="icon-trash icon-white"></i> 
                Delete
        </a>

    </td>
{%endblock%}

{%block createLink %}{{path('sk_admin_entitylistedit')}}{%endblock%}
