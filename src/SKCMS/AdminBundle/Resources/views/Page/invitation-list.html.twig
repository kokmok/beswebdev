{%extends "SKCMSAdminBundle:Element/Template:admin-list.html.twig"%}

{#{%block adminContent%}#}

{#<a href="{{path('sk_admin_edit',{entity:entityParams.name})}}" class="btn btn-large btn-primary">Créer {{entityParams.beautyName}}</a>#}

{%block header2 %}Invitations {{parent()}}{%endblock%}

{%block th %}
<th>Email</th>
<th>Code</th>
<th>Maximum Occurences</th>
<th>Occurences</th>
<th>sent</th>
<th>creator</th>
{{parent()}}
{%endblock%}

{%block tr %}
    <td>{{entity.email}}</td>
    <td>{{entity.code}}</td>
    <td>{{entity.maxIterations}}</td>
    <td>{{entity.iterations}}</td>
    <td>{{entity.sent}}</td>
    <td>{{entity.user.username}}</td>


{#                                    {%endfor%}#}
    <td class="center">
        <a class="btn btn-success" href="#">
                <i class="icon-zoom-in icon-white"></i>  
                View                                            
        </a>
        <a class="btn btn-info" href="{{path('sk_admin_invitationedit',{id:entity.id})}}">
                <i class="icon-edit icon-white"></i>  
                Edit                                            
        </a>
        <a class="btn btn-danger btn-setting" data-content="Voulez vous vraiment supprimer cette entité ?" data-title="Confirmation de suppression" data-confirm="Oui" href="{{path('sk_admin_invitationdelete',{id:entity.id})}}">
                <i class="icon-trash icon-white"></i> 
                Delete
        </a>

    </td>
{%endblock%}

{%block createLink %}{{path('sk_admin_invitationedit')}}{%endblock%}
